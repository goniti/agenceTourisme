import { useState } from 'react'
import { GoTasklist, GoX } from 'react-icons/go'

import './modal.scss'

const Modal = (handleSearch, handleSelect, handleSubmit, autoSuggest, zoneData, title, error) => {
	const [openSuggest, setOpenSuggest] = useState(false)

	return (
		<div className="modal">
			<h1>d</h1>
		</div>
	)
}
export default Modal
