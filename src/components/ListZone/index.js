import './listzone.scss'

const ListZone = () => {
	// return (
	// 	<ul>
	// 		{zonelist.data.map((zone) => (
	// 			<li key={zone.id}>{zone.title}</li>
	// 		))}
	// 	</ul>
	// )
}

export default ListZone
